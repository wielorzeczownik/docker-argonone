# Configuration for per-release notes shown on the GitHub release page and for
# determining the next version bump (user-facing changes only).
# The cumulative CHANGELOG.md file uses cliff.toml.

[changelog]
header = ""
body = """
{%- if version -%}
# Release Notes for Version {{ version }}

**Release Date: {{ timestamp | date(format="%Y-%m-%d") }}**

### What's Changed
{% for group, grouped_commits in commits | group_by(attribute="group") %}
#### {{ group }}

{% for commit in grouped_commits -%}
- {% if remote.github.owner and remote.github.repo %}[{{ commit.id | truncate(length=7, end="") }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/commit/{{ commit.id }}){% else %}{{ commit.id | truncate(length=7, end="") }}{% endif %} {{ commit.message | split(pat="\n") | first | trim }}{% if commit.remote.username %} by {% if commit.remote.username is ending_with("[bot]") %}[@{{ commit.remote.username | replace(from="[bot]", to="") }}](https://github.com/apps/{{ commit.remote.username | replace(from="[bot]", to="") }}){% else %}@{{ commit.remote.username }}{% endif %}{% endif %}{% if commit.remote.pr_number %} in [#{{ commit.remote.pr_number }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/pull/{{ commit.remote.pr_number }}){% endif %}
{% endfor %}
{%- endfor %}
{%- set first_timers = github.contributors | filter(attribute="is_first_time", value=true) %}
{%- if first_timers | length > 0 %}
#### New Contributors

{% for contributor in first_timers -%}
- {% if contributor.username is ending_with("[bot]") %}[@{{ contributor.username | replace(from="[bot]", to="") }}](https://github.com/apps/{{ contributor.username | replace(from="[bot]", to="") }}){% else %}@{{ contributor.username }}{% endif %} made their first contribution{% if contributor.pr_number %} in [#{{ contributor.pr_number }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/pull/{{ contributor.pr_number }}){% endif %}
{% endfor %}
{%- endif %}
{%- if previous.version and remote.github.owner and remote.github.repo %}
---
**Full Changelog:** [{{ previous.version }}...{{ version }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/compare/{{ previous.version }}...{{ version }})
{% elif previous.version %}
---
**Full Changelog:** {{ previous.version }}...{{ version }}
{% endif %}
{% endif %}
"""
trim = true

[git]
commit_preprocessors = [{ pattern = '(?m) +\(#[0-9]+\)$', replace = "" }]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_parsers = [
  { message = "^chore\\(release\\)", skip = true },
  { message = "^Merge ", skip = true },
  { breaking = true, group = "Breaking Changes" },
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactoring" },
  { message = "^chore\\(deps\\)", group = "Dependencies" },
  { message = "^docs", skip = true },
  { message = "^test", skip = true },
  { message = "^style", skip = true },
  { message = "^build", skip = true },
  { message = "^ci", skip = true },
  { message = "^chore", skip = true },
  { message = ".*", group = "Other" },
]
protect_breaking_commits = true
filter_commits = true
tag_pattern = "v[0-9]*"
skip_tags = "v0.0.0"
ignore_tags = ""
topo_order = false
sort_commits = "oldest"

[bump]
features_always_bump_minor = true
breaking_always_bump_major = true
initial_tag = "v0.1.0"
